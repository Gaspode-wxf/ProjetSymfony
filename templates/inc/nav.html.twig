<nav class="navbar shadow-lg bg-base-300 text-neutral-content rounded-box justify-around">

        <a id="lien" href="{{ path('main_accueil_deconecte') }}" title="Accueil">Accueil</a>
        <a id="lien" href="{{ path('main_accueil_conecte') }}" title="AccueilCo">Accueil Connecté</a>
        <a id="lien" href="{{ path('app_login') }}" title="Connexion">Connexion</a>
        <a id="lien" href="{{ path('participant_new') }}" title="createUser">Inscription</a>
        <a id="lien" href="{{ path('sortie_new') }}" title="CreateSortie">Créer une sortie</a>
        <a id="lien" href="{{ path('sortie_index') }}" title="DetailsSortie">Détails de la sortie</a>
        <a id="lien" href="{{ path('admin_index') }}" title="DetailsSortie">Accès Admin</a>
        {% if app.user %}
        <a id="lien" href="{{ path('app_logout') }}" title="Deconnexion">Déconnexion</a>
        {% endif %}

</nav>
